# Landmark Accessibility Standards

## Requirements
- Use semantic landmarks: `header`, `nav`, `main`, `aside`, `footer`
- Only one `main` per page
- Provide `aria-label` for multiple instances

## Test Checklist
- One `main` landmark exists
- Repeated landmarks have unique `aria-label`s
- Landmark usage matches page structure and navigation needs
