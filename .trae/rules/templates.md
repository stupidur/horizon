# Templates Standards

## Guidelines
- Use semantic HTML and include `{% schema %}` where required
- Keep logic in `{% liquid %}`; avoid inline scripts unless scoped
- Lazy-load images and optimize structure

