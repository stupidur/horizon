# Switch Accessibility Standards

## Requirements
- Use checkbox with `role="switch"`
- Manage `aria-checked` and label association
- Keyboard toggle via Space/Enter

## Test Checklist
- Control is a checkbox with `role="switch"`
- `aria-checked` reflects on/off state
- Control is correctly labeled and announced
- Space/Enter toggles the switch via keyboard
